{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Article Templates Settings",
  "description": "Configuration for the Article Templates application",
  "properties": {
    "purgeIntervalDays": {
      "type": "integer",
      "title": "Purge interval (days)",
      "description": "Number of days to keep deleted templates in the trash before permanent deletion. 7 by default.",
      "default": 7,
      "minimum": 0,
      "x-scope": "GLOBAL"
    }
  }
}
